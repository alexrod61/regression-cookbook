# Revision rationale for the running case study, study design, and data wrangling

## Scope
- Target chapter/file: book/13-generalized-poisson.qmd (branch Writing-for-Section-13).
- Exact section(s): replaced `## Case Study 1: Horseshoe Crab Satellites (Overdispersion) {#sec-cs1-crabs}` and `## Data Collection and Wrangling {#sec-cs1-wrangling}` (incl. the draft's stray trailing `:::`) with `{#sec-generalized-poisson-case-study}`, `{#sec-generalized-poisson-study-design}` (+ `{#sec-generalized-poisson-inferential-inquiry}`, `{#sec-generalized-poisson-predictive-inquiry}`), and `{#sec-generalized-poisson-data-wrangling}`.
- Reference chapters/files reviewed: finished Chapter 10 on main L254-606 (study design + wrangling conventions, mirrored deliberately), skill.md sections 6-9, finished Chapter 1 (workflow cross-refs @sec-ds-workflow-*, @fig-ds-workflow-*).

## What was preserved
- The draft's decision to reuse the horseshoe crab dataset as the chapter's running case (kept and strengthened: the reason is now explicit continuity with Chapter 10's failed diagnostics).
- The draft's core observation that the crab counts are overdispersed (the actual numeric confirmation moves to EDA, Stage S4, per the workflow rule that response-distribution checks use training data after the split).
- The finished Chapter 10 wrangling code verbatim (same raw URL, same renames, same factor labels), since analysing identical observations is the foundation of the cross-chapter comparison.

## What changed
- Structural: removed the "Case Study 1 / Case Study 2" numbering (approved single-running-case architecture; B-Line becomes the practice-exercises coding case in Stage S14). Added the previously missing Study Design section with the two-inquiry framing (tables + blockquoted inquiry statements + Heads-up callout), copying the finished Chapter 10 inquiry questions verbatim on purpose, so the model comparison happens on the same scientific questions.
- Statistical content: removed the draft's stale, hand-stated numbers (mean 2.92 / variance 9.91 / ratio 3.4 and "70/30 split from Chapter 10, seed 1046"). These came from the superseded pre-revision Chapter 10 draft; the finished Chapter 10 uses a 50/50 split with seed 123 via rsample, giving train n=86, test n=87, training mean 2.849, variance 9.612 (verified by execution in Stage 0). All chapter numbers now come from executed code in the finished-chapter workflow.
- Wrangling section: replaced the asbio-based load + pre-split overdispersion confirmation with (a) provenance paragraph with verified citations (@brockmann1996, @agresti2013, raw-vs-browser GitHub links), (b) the wrangled-variables Markdown table, (c) static Code tabs + executed Output tabs (R: full dataset via print_as_dt; Python: first ten rows via scrollable_table_html + hidden R chunk, per skill.md section 16.3), and (d) executed, visible data-check listings in both languages (lst-label/lst-cap) reporting dimensions, storage types, missingness, category frequencies, and the response range/zero counts (62 zeros in 173 rows; range 0-15).
- Fixed the draft's leftover stray `:::` after the wrangling narrative.
- Provenance of images: measurement.png used with the exact credit line carried by finished Chapter 10; question-2.png/crab.png are absent from the branch's book/img/, so no decorative images were introduced that lack verified credits (skill.md section 28).

## Why the changes help
- Workflow correctness (skill.md sections 6-9): study design now precedes any modelling; the two inquiries are explicit; response-distribution claims wait for EDA on training data; split details are no longer asserted from a superseded chapter.
- Reproducibility: every displayed number is computed at build time; both languages load from the same public raw file with identical recoding.
- Parity with finished Chapter 10 verified by execution this stage: R and Python wrangling both produce 173 x 5, zero missing values, identical category counts (Light 12, Medium light 95, Medium dark 44, Dark 22; Both good 37, One worn or broken 15, Both worn or broken 121), and the canonical 50/50 split (86/87).

## Execution and validation
- R 4.6.1 (readr/tibble/tidyr/dplyr/rsample installed this environment): /tmp/regref/s3_wrangle.R executed successfully against the live GitHub raw file; results quoted above.
- Python 3 (.venv): /tmp/regref/s3_wrangle.py produced identical dimensions, category counts, and zero counts.
- No Quarto render attempted at this stage (render QC = Stage S15). Downstream stale sections (old `## Data Modelling` onward) still reference the draft's old train_set/test_set objects; they are replaced in Stages S4-S12 — an acknowledged, documented state of the intermediate integrated file per the iterative process.

## What should be avoided
- asbio::crabs loading; 70/30 or seed-1046 splits; pre-split mean/variance "confirmations" presented as model evidence; "satell"/"width" old column names; hardcoded output text blocks in place of executed chunks; the {#sec-cs1-*} anchors.

## References to add or verify
- Appended this stage (verbatim from main's bibliography): @brockmann1996 (Ethology 102(1):1-21, doi:10.1111/j.1439-0310.1996.tb01099.x), @agresti2013 (Categorical Data Analysis, Wiley, 2013).

## Placement and dependencies
- Replacement spans from the old `## Case Study 1` heading through the end of the old wrangling section (before `## Data Modelling`).
- Objects introduced for later stages: `crabs` (R), `crabs` (Python), `crabs_head_py_html`. Stage S4 introduces the canonical split `training_data`/`testing_data` (rsample, prop=0.5, set.seed(123)) plus the reticulate transfer, then all EDA on training data. Stages S7+ will reuse the {tbl-generalized-poisson-study-design-inquiries} recap in the Results section.
